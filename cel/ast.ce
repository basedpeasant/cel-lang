

TYPE_U8:                   u8 = 1;
TYPE_U16:                  u8 = 2;
TYPE_U32:                  u8 = 3;
TYPE_U64:                  u8 = 4;
TYPE_I8:                   u8 = 5;
TYPE_I16:                  u8 = 6;
TYPE_I32:                  u8 = 7;
TYPE_I64:                  u8 = 8;
TYPE_BOOL:                 u8 = 9;
TYPE_VOID_PTR:             u8 = 10;
TYPE_STRING:               u8 = 11;
TYPE_CUSTOM:               u8 = 12; // TODO probably don't need it, its only for builtin types
TYPE_CUSTOM_DECLARATION:   u8 = 13; // TODO probably don't need it, its only for builtin types

Attribute: type struct {
	
}

Type_Custom: type struct {
	name: *Token,
	attributes: [+]Attribute
}

Type_Custom_Decl: type struct {
	name: *Token,
	fields: [+]Field,
	attributes: [+]Attribute
}

Type_Builtin: type struct {
	kind: u8,
	attributes: [+]Attribute
}

Return_Node: type struct {
    expr: *Expression,
}

Block_Node: type struct {
	statements: [+]Statement,
	scope: u32
}

Type_Proc: type struct {
	attributes: [+]Attribute,
	args: [+]Variable_Decl_Node,
	return_type: *Type,
	name: *Token,
	block: Block_Node,
}

Type_Array: type struct {
    size: *Expression,
    array_type: *Type
}

Type_Dynamic_Array: type struct {
	
}

Type_Choice: type struct {
	
}

Type_Pointer: type struct {
	inner: *Type,
}

Type: type choice {
	Type_Builtin,
	Type_Pointer,
	Type_Proc,
	Type_Array,
	Type_Dynamic_Array,
	Type_Choice,
	Type_Custom_Decl,
	Type_Custom,
}

Field: type struct {
	name: *Token,
	type_name: Type,
}

String_Literal: type struct {
	str: string,
    index: i32
}

Strings_Map: type struct {
	key: *u8,
	value: String_Literal
}

Variable_Decl_Node: type struct {
	symbol: *Token,
	rhs: *Expression,
	var_type: Type,
	is_arg: bool
}

Variable_Node: type struct {
    symbol: *Token, 
}

//VarDecl_Map: type struct {
//	key: *u8,
//	value: Variable_Decl_Node
//}

Scope: type struct {
	parent_scope: i32,
	id: i32,
	map: [+]Variable_Decl_Node
}

Number_Node: type struct {
	val: i64
}

OP_ADD: i32 = 10;
OP_SUB: i32 = 11;
OP_MUL: i32 = 12;
OP_DIV: i32 = 13;
OP_OR: i32 = 14;
OP_LOGICAL_OR: i32 = 15;
OP_GT: i32 = 16;
OP_GTE: i32 = 17;
OP_LT: i32 = 18;
OP_LTE: i32 = 19;
OP_EQUAL: i32 = 20;
OP_NOT_EQUAL: i32 = 21;
OP_REFERENCE: i32 = 22;
OP_ASSIGN: i32 = 23;
OP_ARRAY_INDEX: i32 = 24;
OP_ACCESS: i32 = 25;
OP_NOT: i32 = 26;
OP_AND: i32 = 27;
OP_LOGICAL_AND: i32 = 28;
OP_LEFT_SHIFT: i32 = 29;
OP_RIGHT_SHIFT: i32 = 30;
OP_BITWISE_XOR: i32 = 31;
OP_BITWISE_NOT: i32 = 32;
OP_ADD_ASSIGN: i32 = 33;
OP_SUB_ASSIGN: i32 = 34;

Unary_Node: type struct {
    op: i32,
    base: *Expression,
    expr: *Expression,
}

Binary_Node: type struct {
    op: i32,
    lhs: *Expression,
    rhs: *Expression
}

Call_Node: type struct {
    name: *Token,
    args: [+]Expression
}

Struct_Init_Node: type struct {
    name: *Token,
    exprs: [+]Expression,
}

Array_Literal: type struct {
    elements: [+]Expression,
    size: u32 // TODO: prob not needed
}

// NOTE: DUE TO THE WAY WE ARE CURRENTLY DOING THIS THE ORDER FOR THIS MUST STAY THE SAME
Expression: type choice {
    Binary_Node,
    Number_Node,
    Variable_Node,
    Call_Node,
    String_Literal,
    Unary_Node,
    Struct_Init_Node,
    Array_Literal,
    // Index(IndexNode),
    // Variable(VariableNode),
    // Call(CallNode),
    // String(StringLiteral),
    // Char(CharLiteral),
    // Array(ArrayLiteral),
    // Struct(StructDeclarationNode),
    // Reference(Box<Expression>), // TODO: this should actually be unary node
    // Not(Box<Expression>), // TODO: this should actually be unary node
}

If_Node: type struct {
    block: Block_Node,
    condition: *Expression,
    next: *If_Node,
    is_else: bool
}

For_Node: type struct {
	block: Block_Node,
	is_classic_for: bool,
	init: *Statement,
	condition: *Expression,
    post: *Expression
}

Expression_Statement_With_Block: type choice {
	If_Node,
	For_Node,
	// MatchNode,
}

Expression_Statement: type choice {
	// ReturnNode,
	Expression,
	// DeferNode,
	Expression_Statement_With_Block
}

Proc_Header_Node: type struct {
	name: *Token,
	// block: Block_Node,
	args: [+]Variable_Decl_Node,
	return_type: *Type,
	// attributes: [+]Attribute // need enum support
}

Decl_Node: type choice {
	// Proc_Node,
	Variable_Decl_Node
}

Statement: type choice {
	Expression_Statement,
	Decl_Node,
    Return_Node,
	// Break,
	// Continue
}

Ast: type struct {
	root_block: Block_Node,
	tokens: [+]Token,
	index: u32,
	scopes: [+]Scope,
    types: [+]Type,
	strings: [+]String_Literal,
    strings_index: i32,
	current_scope: u32,
    procs: [+]Proc_Header_Node
}

find_name_in_scope: proc(ast: *Ast, scope_id: i32, name: string) -> *Variable_Decl_Node {
	size: i32 = array_size(ast.scopes);
	if scope_id < 0 || scope_id >= size {
		return 0;
	}
	scope: Scope = ast.scopes[scope_id];

	for i: i32 = 0; i < size; i = i + 1 {
		// var_decl: Variable_Decl_Node = scope.map[i];
		// if strcmp(name, var_decl.symbol.tok) {
			// return &scope.map[i];
		// }
	}
	return 0;
}

create_new_scope: proc(ast: *Ast, parent_scope: i32) -> i32 {
	scope: Scope = Scope{parent_scope, array_size(ast.scopes), array_new(Variable_Decl_Node)};
	array_append(ast.scopes, scope);
	return array_size(ast.scopes) - 1;
}

get_current_token: proc(ast: *Ast) -> *Token {
	if ast.index >= array_size(ast.tokens) {
		return 0;
	}
	return &ast.tokens[ast.index];
}

get_peek: proc(ast: *Ast) -> *Token {
	if ast.index + 1 >= array_size(ast.tokens) {
		return 0;
	}
	return &ast.tokens[ast.index + 1];
}

ast_match: proc(ast: *Ast, tt: u8, file: string, line: i32) {
	// file and line are for debugging purposes
	current_token: *Token = get_current_token(ast);
    current_token_str: string = current_token.tok;
    tt_str: string = get_tt_string_repr(current_token.tt);
    tt_expected: string = get_tt_string_repr(tt);
    filename: string = current_token.filename;
	if current_token.tt != tt {
		printf("[ERROR]: Expected %s but got %s ('%s') on %s:%d\n".ptr, tt_expected.ptr, tt_str.ptr, current_token_str.ptr, filename.ptr, current_token.y);
		panic("Compiler failed", file, line);
	}
}

advance: proc(ast: *Ast) {
	ast.index = ast.index + 1;
}

// NOTE: we use Type instead of *Type because we don't support matching with 
//       choice types that are pointers and also it doesn't matter having a copy
//       here anyway
// TODO: uncomment out this once below match is fixed
get_type_string: proc(type__: Type) -> string {
   // NOTE: have to redeclare cus of a bug lol
   type_: Type = type__;
   match x := type_ {
       Type_Builtin => {
           kind: i32 = x.kind;
           if kind == TYPE_U8 {
               return "u8";
           } else if kind == TYPE_U16 {
               return "u16";
           } else if kind == TYPE_U32 {
               return "u32";
           } else if kind == TYPE_U64 {
               return "u64";
           } else if kind == TYPE_I8 {
               return "i8";
           } else if kind == TYPE_I16 {
               return "i16";
           } else if kind == TYPE_I32 {
               return "i32";
           } else if kind == TYPE_I64 {
               return "i64";
           } else if kind == TYPE_STRING {
               return "string";
           } else {
               printf("\n%d\n".ptr, kind);
               todo("implement get_type_string for Type_Builtin", #FILE, #LINE);
           }
       }
       Type_Pointer => { 
           todo("implement get_type_string for Type_Pointer", #FILE, #LINE);
       }
       Type_Proc => { 
           todo("implement get_type_string for Type_Proc", #FILE, #LINE);
       }
       Type_Array => { 
           todo("implement get_type_string for Type_Array", #FILE, #LINE);
       }
       Type_Dynamic_Array => { 
           todo("implement get_type_string for Type_Dynamic_Array", #FILE, #LINE);
       }
       Type_Choice => { 
           todo("implement get_type_string for Type_Choice", #FILE, #LINE);
       }
       Type_Custom_Decl => {
           todo("implement get_type_string for Type_Custom_Decl", #FILE, #LINE);
       }
       Type_Custom => { 
           todo("implement get_type_string for Type_Custom", #FILE, #LINE);
       }
   }
}

get_type: proc(token: *Token) -> Type {
	if strcmp(token.tok, "u8") {
		inner: Type_Builtin = Type_Builtin{ TYPE_U8, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "u16") {
		inner: Type_Builtin = Type_Builtin{ TYPE_U16, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "u32") {
		inner: Type_Builtin = Type_Builtin{ TYPE_U32, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "u64") {
		inner: Type_Builtin = Type_Builtin{ TYPE_U64, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "i8") {
		inner: Type_Builtin = Type_Builtin{ TYPE_I8, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "i16") {
		inner: Type_Builtin = Type_Builtin{ TYPE_I16, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "i32") {
		inner: Type_Builtin = Type_Builtin{ TYPE_I32, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "i64") {
		inner: Type_Builtin = Type_Builtin{ TYPE_I64, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "bool") {
		inner: Type_Builtin = Type_Builtin{ TYPE_BOOL, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "void_ptr") {
		inner: Type_Builtin = Type_Builtin{ TYPE_VOID_PTR, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "string") {
		inner: Type_Builtin = Type_Builtin{ TYPE_STRING, array_new(Attribute) };
		ret: Type = Type{ inner };
		return ret;
	} else if strcmp(token.tok, "proc") {
		inner: Type_Proc = Type_Proc{ array_new(Attribute), array_new(Variable_Decl_Node), 0 };
		ret: Type = Type{ inner };
		return ret;
	} else {
        inner: Type_Custom = Type_Custom{ token, array_new(Attribute) };
        ret: Type = Type{ inner };
        return ret;
	}
}



extract_type: proc(ast: *Ast) -> Type {
	current_token: *Token = get_current_token(ast);
	if current_token.tt == TK_WORD {
		// TODO: certain types need to be modified after get_type
		//       e.g. pointers, procs, arrays, etc
		//       types like string, void_ptr, u8, etc don't 
		t: Type = get_type(current_token);
        // TODO: need to be able to handle attributes
		match _x := t {
			Type_Builtin => {
                advance(ast);
                return t;
			}, // do nothing 
			Type_Pointer => {
				todo("handling for Type_Pointer", #FILE, #LINE);
			},
			Type_Array => {
				todo("handling for Type_Array", #FILE, #LINE);
			},
			Type_Proc => {
				unreachable("handled else where since 'proc' can never be a TK_WORD", #FILE, #LINE);
			}
			Type_Dynamic_Array => {
				todo("handling for Type_Dynamic_Array", #FILE, #LINE);
			},
			Type_Choice => {
				todo("handling for Type_Choice", #FILE, #LINE);
			},
            Type_Custom_Decl => {
				unreachable("handled else where since 'type' can never be a TK_WORD", #FILE, #LINE);
            },
			Type_Custom => {
                advance(ast);
                return t;
                // do nothing?
			}
		}
	} else if current_token.tt == TK_PROC {
		advance(ast);
		ast_match(ast, TK_OPEN_PAREN, #FILE, #LINE);
		advance(ast);
        current_token: *Token = get_current_token(ast);
		attributes: [+]Attribute; //TODO: need to populate as well

		args: [+]Variable_Decl_Node;
        for true {
            current_token = get_current_token(ast);
            if current_token.tt == TK_CLOSE_PAREN {
                break;
            } else if current_token.tt == TK_COMMA {
                advance(ast);
                current_token = get_current_token(ast);
            }
            symbol: *Token = current_token;
            advance(ast);
            ast_match(ast, TK_COLON, #FILE, #LINE);
            advance(ast);
            var_decl: Variable_Decl_Node = Variable_Decl_Node{ symbol, 0, extract_type(ast), true };
            array_append(args, var_decl);
        }
		ast_match(ast, TK_CLOSE_PAREN, #FILE, #LINE);
		advance(ast);

        current_token = get_current_token(ast);
		return_type: *Type = 0;
        if current_token.tt == TK_ARROW {
            return_type = calloc(1, sizeof(Type));
            // return type
            advance(ast);
            type_: Type = extract_type(ast);
            memcpy(return_type, &type_, sizeof(Type));
        }
		ast_match(ast, TK_OPEN_CURLY, #FILE, #LINE);
		advance(ast);
		proc_block: Block_Node = create_block(ast, false, ast.current_scope);
        // TODO: need to add the variable_decls into the proc_block scope
		ast_match(ast, TK_CLOSE_CURLY, #FILE, #LINE);

		proc_type: Type_Proc = Type_Proc{ attributes, args, return_type, 0, proc_block };
		ret: Type = Type { proc_type };
		return ret;
		// todo("handling for Type_Proc", #FILE, #LINE);
	} else if current_token.tt == TK_TYPE {
        // definition of a new type struct
        advance(ast);
        current_token: *Token = get_current_token(ast);
        if current_token.tt == TK_STRUCT {
            fields: [+]Field = array_new(Field);
            advance(ast);
            ast_match(ast, TK_OPEN_CURLY, #FILE, #LINE);
            advance(ast);
            for true {
                current_token: *Token = get_current_token(ast);
                if current_token.tt == TK_CLOSE_CURLY {
                    break;
                }
                ast_match(ast, TK_WORD, #FILE, #LINE);
                name: *Token = current_token; 
                advance(ast);
                ast_match(ast, TK_COLON, #FILE, #LINE);
                // TODO: handle attributes
                advance(ast);
                field_type: Type = extract_type(ast);
                current_token = get_current_token(ast);
                if current_token.tt == TK_COMMA {
                    advance(ast);
                } else if current_token.tt != TK_CLOSE_CURLY {
                    current_token_str: string = current_token.tok;
                    printf("%s\n".ptr, current_token_str.ptr);
                    panic("Seems you have missed a comma!", #FILE, #LINE);
                }
                field: Field = Field{ name, field_type };
                array_append(fields, field);
            }
            struct_type: Type_Custom_Decl = Type_Custom_Decl{ 0, fields, array_new(Attribute) };
            ret: Type = Type{ struct_type }; // set the name later after extract_type
            return ret;
        } else if current_token.tt == TK_CHOICE {
            todo("implement choice types", #FILE, #LINE);
        }
      } else if current_token.tt == TK_STAR {
          // TODO: why is this here lol
          advance(ast);
          inner_type: *Type = calloc(1, sizeof(Type));
          tmp: Type = extract_type(ast);
          memcpy(inner_type, &tmp, sizeof(Type));
          ptr: Type_Pointer = Type_Pointer{ inner_type };
          ret: Type = Type{ ptr };
          return ret;
	} else if current_token.tt == TK_OPEN_SQUARE {
        // Array type
        advance(ast);
        current_token = get_current_token(ast);
        if current_token.tt == TK_PLUS {
            todo("implement dynamic arrays", #FILE, #LINE);
        } else if current_token.tt == TK_CLOSE_SQUARE {
            todo("implement slices", #FILE, #LINE);
        } else if current_token.tt == TK_WORD || current_token.tt == TK_NUMBER || is_operator(current_token) {
            // vla or fixed array 
            size_tmp: Expression = ast_create_expr(ast, ast.current_scope, false);
            size: *Expression = calloc(1, sizeof(Expression));
            memcpy(size, &size_tmp, sizeof(Expression));
            ast_match(ast, TK_CLOSE_SQUARE, #FILE, #LINE);
            advance(ast);
            array_type_tmp: Type = extract_type(ast);
            array_type: *Type = calloc(1, sizeof(Type));
            memcpy(array_type, &array_type_tmp, sizeof(Type));
            array: Type_Array = Type_Array{ size, array_type };
            ret: Type = Type{ array };
            return ret;
        }
    } else {
		current_token_str: string = current_token.tok;
		printf("%s\n".ptr, current_token_str.ptr);
		todo("Unexpected Token", #FILE, #LINE);
	}
}

print_type: proc(var_type: Type) {
	v: Type = var_type;
	// NOTE/TODO: this _x has to be there because of the way the compiler works rn
	// obviously remove it once the match code supports it not being there
	match _x := v {
		Type_Builtin => {
			todo("printing for Type_Builtin", #FILE, #LINE);
		}, 
		Type_Pointer => {
			todo("printing for Type_Pointer", #FILE, #LINE);
		},
		Type_Proc => {
			printf("Type: Proc\n".ptr);
			//todo("printing for Type_Proc", #FILE, #LINE);
		},
		Type_Array => {
			todo("printing for Type_Array", #FILE, #LINE);
		},
		Type_Dynamic_Array => {
			todo("printing for Type_Dynamic_Array", #FILE, #LINE);
		},
		Type_Choice => {
			todo("printing for Type_Choice", #FILE, #LINE);
		},
        Type_Custom_Decl => {
			todo("printing for Type_Custom_Decl", #FILE, #LINE);
        }
		Type_Custom => {
			todo("printing for Type_Custom", #FILE, #LINE);
		}
	}
}

pow: proc(base: i64, exponent: i64) -> i64 {
    if exponent < 0 {
        panic("Language doesn't support negative numbers yet lol", #FILE, #LINE);
    }
    num: i64 = 1;
    for i: i32 = 0; i < exponent; i = i + 1 {
        num = base * num;
    }
    return num;
}

// utility to convert string to i64
// NOTE: this proc assumes that the string is a known integer
// ALSO might not be correct for all text types
strtoi: proc(str: string) -> i64 {
    
    if str.length > 2 && str.ptr[0] == '0' && str.ptr[1] == 'x' {
        // hex number 
        num: i64 = 0;
        base: i32 = 16;
        for i: i32 = str.length - 1; i > 1; i = i - 1 {
            c: u8 = str.ptr[i];

            c_num: i64;
            if c >= 48 && c <= 57 {
                c_num = c - 48;
            } else if c >= 65 && c <= 70 {
                c_num = c - 55;
            } else if c >= 97 && c <= 102 {
                c_num = c - 87;
            } else {
                printf("%c\n".ptr, c);
                unreachable("Should not be here because this should get caught in tokenization", #FILE, #LINE);
            }
            num = num + (c_num * pow(base, str.length - i + 1));
        }
        return num;
    } else {
        // decimal number
        num: i64 = 0;
        base: i32 = 1;
        for i: i32 = str.length - 1; i >= 0; i = i - 1 {
            c: u8 = str.ptr[i];

            c_num: i64 = c - 48;
            if c_num < 0 || c_num > 9 {
                printf("%zu\n".ptr, c);
                panic("invalid digit during strtoi", #FILE, #LINE);
            }

            num = c_num * base + num;
            base = base * 10;
        }
        return num;
    }
}

parse_primary: proc(ast: *Ast, scope: u32, in_if: bool) -> Expression {
    current_token: *Token = get_current_token(ast); 
    if current_token.tt == TK_NUMBER {
        num: Number_Node = Number_Node{ strtoi(current_token.tok) };
        ret: Expression = Expression{ num };
        advance(ast);
        return ret;
    } else if current_token.tt == TK_WORD {
        peek: *Token = get_peek(ast);
        if peek.tt == TK_OPEN_PAREN {
            name: *Token = current_token;
            advance(ast);
            advance(ast);
            args: [+]Expression;
            for true {
                current_token: *Token = get_current_token(ast);
                if current_token.tt == TK_CLOSE_PAREN {
                    break;
                }
                arg: Expression = ast_create_expr(ast, scope, false); 
                array_append(args, arg);
                current_token = get_current_token(ast);
                if current_token.tt == TK_CLOSE_PAREN {
                    break;
                } else if current_token.tt == TK_COMMA {
                    advance(ast);
                }
            }
            ast_match(ast, TK_CLOSE_PAREN, #FILE, #LINE);
            advance(ast);
            call: Call_Node = Call_Node{ name, args };
            ret: Expression = Expression{ call };
            return ret;
        } else if peek.tt == TK_OPEN_CURLY && !in_if {
            // make sure that the program doesn't mistake an if statement and a opencurly
            // as struct initialization list hence why in_if is needed
            // struct initialization list 
            name: *Token = current_token;
            advance(ast); // name
            advance(ast); // opencurly
            current_token = get_current_token(ast);
            custom: Struct_Init_Node = Struct_Init_Node{ name, array_new(Expression) };
            for current_token.tt != TK_CLOSE_CURLY {
                value: Expression = ast_create_expr(ast, scope, false);
                current_token = get_current_token(ast);
                array_append(custom.exprs, value);
                if current_token.tt == TK_COMMA {
                    advance(ast);
                }
            }
            advance(ast);
            ret: Expression = Expression{ custom };
            return ret;
        } else if peek.tt == TK_OPEN_SQUARE {
            variable: Variable_Node = Variable_Node{ get_current_token(ast) };
            base_tmp: Expression = Expression { variable };
            base_alloc: *Expression = calloc(1, sizeof(Expression));
            memcpy(base_alloc, &base_tmp, sizeof(Expression));
            expr_alloc: *Expression = calloc(1, sizeof(Expression));
            op: i32 = get_op(peek); // note: I guess doesn't need to be a function call but anyway
            advance(ast);
            ast_match(ast, TK_OPEN_SQUARE, #FILE, #LINE);
            advance(ast);
            expr: Expression = ast_create_expr(ast, scope, in_if);
            ast_match(ast, TK_CLOSE_SQUARE, #FILE, #LINE);
            advance(ast);
            memcpy(expr_alloc, &expr, sizeof(Expression));
            expr_alloc._tag = get_expr_tag(&expr);
            tmp_str: string = current_token.tok;
            unary: Unary_Node = Unary_Node{ op, base_alloc, expr_alloc }; 
            ret: Expression = Expression{ unary };
            return ret;
        } else {
            // variables
            print_token(get_current_token(ast));
            variable: Variable_Node = Variable_Node{ get_current_token(ast) };
            ret: Expression = Expression{ variable };
            advance(ast);
            tok: *Token = get_current_token(ast);
            return ret;
        }
    } else if current_token.tt == TK_DOUBLE_QUOTE {
        string_literal: String_Literal = String_Literal{ current_token.tok, ast.strings_index };
        array_append(ast.strings, string_literal);
        ret: Expression = Expression{ string_literal };
        ast.strings_index = ast.strings_index + 1;
        advance(ast);
        return ret;
    } else if current_token.tt == TK_OPEN_PAREN {
        advance(ast) ;
        expr: Expression = ast_create_expr(ast, scope, false);
        ast_match(ast, TK_CLOSE_PAREN, #FILE, #LINE);
        advance(ast);
        //TODO: handle postfix operators for bracketed expressions as well when implemented
        return expr;
    } else if current_token.tt == TK_TILDE     || 
              current_token.tt == TK_AMPERSAND || 
              current_token.tt == TK_NOT 
    {
        base_alloc: *Expression = calloc(1, sizeof(Expression));
        op: i32 = get_op(current_token);
        advance(ast);
        expr: Expression = parse_primary(ast, scope, in_if);
        memcpy(base_alloc, &expr, sizeof(Expression));
        base_alloc._tag = get_expr_tag(&expr);
        tmp_str: string = current_token.tok;
        unary: Unary_Node = Unary_Node{ op, base_alloc, 0 }; 
        ret: Expression = Expression{ unary };
        return ret;
    } else if current_token.tt == TK_OPEN_SQUARE {
        // array literal
        ast_match(ast, TK_OPEN_SQUARE, #FILE, #LINE);
        advance(ast);
        array_lit: Array_Literal;
        array_lit.elements = array_new(Expression);
        current_token: *Token = get_current_token(ast);
        for true {
            if current_token.tt == TK_CLOSE_SQUARE {
                break;
            } else if current_token.tt == TK_COMMA {
                advance(ast);
            }
            element: Expression = ast_create_expr(ast, scope, false);
            array_append(array_lit.elements, element);
            current_token = get_current_token(ast);
        }
        ast_match(ast, TK_CLOSE_SQUARE, #FILE, #LINE);
        advance(ast);
        ret: Expression = Expression{ array_lit };
        return ret;
    } else {
        print_token(current_token);
        breakpoint(0);
        panic("(Parse Primary) Unexpected Token", #FILE, #LINE);
    }
}

get_op_string: proc(op: i32) -> string {
    if op == OP_ADD {
        return "+";
    } else if op == OP_SUB {
        return "-";
    } else if op == OP_MUL {
        return "*";
    } else if op == OP_DIV {
        return "/";
    } else if op == OP_OR {
        return "|";
    } else if op == OP_LOGICAL_OR {
        return "||";
    } else if op == OP_LOGICAL_AND {
        return "&&";
    } else if op == OP_GT {
        return ">";
    } else if op == OP_GTE {
        return ">=";
    } else if op == OP_LT {
        return "<";
    } else if op == OP_LTE {
        return "<=";
    } else if op == OP_EQUAL {
        return "==";
    } else if op == OP_NOT_EQUAL {
        return "!=";
    } else if op == OP_REFERENCE {
        return "&";
    } else if op == OP_ASSIGN {
        return "=";
    } else if op == OP_ARRAY_INDEX {
        return "[]";
    } else if op == OP_ACCESS {
        return ".";
    } else if op == OP_NOT {
        return "!";
    } else if op == OP_AND {
        return "&";
    } else if op == OP_LEFT_SHIFT {
        return "<<";
    } else if op == OP_RIGHT_SHIFT {
        return ">>";
    } else if op == OP_BITWISE_XOR {
        return "^";
    } else if op == OP_BITWISE_NOT {
        return "~";
    } else if op == OP_ADD_ASSIGN {
        return "+=";
    } else if op == OP_SUB_ASSIGN {
        return "-=";
    } else {
        todo("print for get_op_string", #FILE, #LINE);
    }
}

// NOTE: this is because we don't have negative numbers
PREC_SENTINEL: i32 = 1024;

get_op: proc(token: *Token) -> i32 {
    if token.tt == TK_PLUS {
        return OP_ADD;
    } else if token.tt == TK_SUB {
        return OP_SUB;
    } else if token.tt == TK_STAR {
        return OP_MUL;
    } else if token.tt == TK_SLASH {
        return OP_DIV;
    } else if token.tt == TK_OR {
        return OP_OR;
    } else if token.tt == TK_LOGICAL_OR {
        return OP_LOGICAL_OR;
    } else if token.tt == TK_LOGICAL_AND {
        return OP_LOGICAL_AND;
    } else if token.tt == TK_GT {
        return OP_GT;
    } else if token.tt == TK_GTE {
        return OP_GTE;
    } else if token.tt == TK_LT {
        return OP_LT;
    } else if token.tt == TK_LTE {
        return OP_LTE;
    } else if token.tt == TK_EQUAL {
        return OP_EQUAL;
    } else if token.tt == TK_NOT_EQUAL {
        return OP_NOT_EQUAL;
    } else if token.tt == TK_AMPERSAND {
        return OP_REFERENCE;
    } else if token.tt == TK_ASSIGN {
        return OP_ASSIGN;
    } else if token.tt == TK_OPEN_SQUARE {
        return OP_ARRAY_INDEX;
    } else if token.tt == TK_DOT {
        return OP_ACCESS;
    } else if token.tt == TK_NOT {
        return OP_NOT;
    } else if token.tt == TK_AMPERSAND {
        return OP_AND;
    } else if token.tt == TK_LEFT_SHIFT {
        return OP_LEFT_SHIFT;
    } else if token.tt == TK_RIGHT_SHIFT {
        return OP_RIGHT_SHIFT;
    } else if token.tt == TK_HAT {
        return OP_BITWISE_XOR;
    } else if token.tt == TK_TILDE {
        return OP_BITWISE_NOT;
    } else if token.tt == TK_ADD_ASSIGN {
        return OP_ADD_ASSIGN;
    } else if token.tt == TK_SUB_ASSIGN {
        return OP_SUB_ASSIGN;
    } else {
        str: string = token.tok;
        printf("%s\n".ptr, str.ptr);
        panic("Unknown Operator", #FILE, #LINE);
    }
}

get_prec: proc(ast: *Ast, token: *Token) -> i32 {
    // TODO: other ops
    if token.tt == TK_OPEN_SQUARE || token.tt == TK_DOT {
        return 13;
    } else if token.tt == TK_TILDE {
        return 12;
    } else if token.tt == TK_STAR || token.tt == TK_SLASH {
        return 11;
    } else if token.tt == TK_PLUS || token.tt == TK_SUB {
        return 10;
    } else if token.tt == TK_LEFT_SHIFT || token.tt == TK_RIGHT_SHIFT {
        return 9;
    } else if token.tt == TK_GT || token.tt == TK_GTE {
        return 8;
    } else if token.tt == TK_LT || token.tt == TK_LTE {
        return 8;
    } else if token.tt == TK_HAT {
        return 7;
    } else if token.tt == TK_ASSIGN     || 
              token.tt == TK_ADD_ASSIGN || 
              token.tt == TK_SUB_ASSIGN 
    {
        return 2;
    } else if token.tt == TK_SEMICOLON    || 
              token.tt == TK_COMMA        ||
              token.tt == TK_CLOSE_PAREN  ||
              token.tt == TK_CLOSE_SQUARE ||
              token.tt == TK_OPEN_CURLY   ||
              token.tt == TK_CLOSE_CURLY  
    {
        return PREC_SENTINEL; 
    } else {
        str: string = token.tok;
        printf("Unknown Operator '%s'\n".ptr, str.ptr);
        panic("", #FILE, #LINE);
    }
}

// NOTE/TODO: THIS IS EXTREMELY HACKY AND MUST BE CHANGED AS SOON AS BETTER DX FOR CHOICES APPEAR
// NOTE: ORDER MUST NOT CHANGE
get_expr_tag: proc(expr_: *Expression) -> i32 {
    expr: *Expression = expr_;
    match _e := expr {
        Binary_Node => {
            return 0;
        }
        Number_Node => {
            return 1;
        },
        Variable_Node => {
            return 2;
        },
        Call_Node => {
            return 3;
        },
        String_Literal => {
            return 4;
        },
        Unary_Node => {
            return 5;
        },
        Struct_Init_Node => {
            return 6;
        },
        Array_Literal => {
            return 7;
        }
    }
}

ast_create_binary: proc(op: i32, lhs: Expression, rhs: Expression) -> Binary_Node {
    lhs_alloc: *Expression = calloc(1, sizeof(Expression));
    rhs_alloc: *Expression = calloc(1, sizeof(Expression));
    memcpy(lhs_alloc, &lhs, sizeof(Expression));
    memcpy(rhs_alloc, &rhs, sizeof(Expression));
    lhs_alloc._tag = get_expr_tag(&lhs);
    rhs_alloc._tag = get_expr_tag(&rhs);
    ret: Binary_Node = Binary_Node{op, lhs_alloc, rhs_alloc};
    return ret;
}

ast_create_expr_with_prec: proc(ast: *Ast, min_prec: i32, scope: u32, in_if: bool) -> Expression {
    lhs: Expression = parse_primary(ast, scope, in_if);
    // TODO: parse_porstfix
    for true {
        op_token: *Token = get_current_token(ast);
        if op_token == 0 {
            break;
        }
        prec: i32 = get_prec(ast, op_token);

        // NOTE: this is because we don't have negative numbers lol
        if prec == PREC_SENTINEL || prec < min_prec {
            break;
        }

        // TODO: check if this actually succeeded
        advance(ast);
        // if success { // TODO: something like this
        //     return lhs
        // }

        rhs: Expression = ast_create_expr_with_prec(ast, prec, scope, in_if);
   
        bin: Binary_Node = ast_create_binary(get_op(op_token), lhs, rhs);
        tmp: Expression = Expression {bin};
        lhs = tmp;

        // TODO : parse_postfix
    }
    return lhs;
}

ast_create_expr: proc(ast: *Ast, scope: u32, in_if: bool) -> Expression {
    return ast_create_expr_with_prec(ast, 0, scope, in_if);
}

ast_create_for: proc(ast: *Ast, scope: u32) -> For_Node {
    ast_match(ast, TK_FOR, #FILE, #LINE);
    advance(ast);

    // TODO: account for other types of for loops
    peek: *Token = get_peek(ast);
    is_classic_for: bool = false;
    expr: *Expression = 0;
    init: *Statement = 0; // TODO: figure out how to get this to work lol
    post: *Expression = 0;
    for_scope: u32 = scope;

    current_token: *Token = get_current_token(ast);
    if current_token.tt == TK_OPEN_CURLY {
        // forever loop
        advance(ast);
        block: Block_Node = create_block(ast, false, scope);
        for_node: For_Node = For_Node{ block, is_classic_for, init, expr, post };
        return for_node;
    }

    if peek.tt == TK_COLON {
        // classical for loop 
        // TODO: this won't allow for empty conditions in the classic for 
        for_scope = create_new_scope(ast, for_scope);
        init = create_statement(ast, for_scope);
        ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
        advance(ast);

        expr_tmp: Expression = ast_create_expr(ast, scope, true);
        expr = calloc(1, sizeof(Expression));
        memcpy(expr, &expr_tmp, sizeof(Expression));
        ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
        advance(ast);

        post_tmp: Expression = ast_create_expr(ast, scope, true);
        post = calloc(1, sizeof(Expression));
        memcpy(post, &post_tmp, sizeof(Expression));
        is_classic_for = true;
    } else {
        // while for loops
        expr_tmp: Expression = ast_create_expr(ast, scope, true);
        expr = calloc(1, sizeof(Expression));
        memcpy(expr, &expr_tmp, sizeof(Expression));
    }
    ast_match(ast, TK_OPEN_CURLY, #FILE, #LINE);
    advance(ast);

    block: Block_Node = create_block(ast, false, for_scope);
    for_node: For_Node = For_Node{ block, is_classic_for, init, expr, post };
    return for_node;
}

ast_create_if: proc(ast: *Ast, scope: u32) -> If_Node {
    skip_condition: bool = false;
    is_else: bool = false;
    current_token: *Token = get_current_token(ast);
    peek: *Token = get_peek(ast);

    //TODO/NOTE: would be cool allowing else to have one statement without the curly braces
    if current_token.tt == TK_IF {
        advance(ast);
    } else if current_token.tt == TK_ELSE && peek.tt == TK_OPEN_CURLY {
        // first if
        skip_condition = true; 
        is_else = true;
        advance(ast);
    } else if current_token.tt == TK_ELSE && peek.tt == TK_IF {
        // else 
        is_else = true;
        advance(ast);
        advance(ast);
    }
    
    condition: *Expression = 0;
    // TODO/NOTE: probably could check if there is any condition here anyway
    // this would just be a good addition for error messages
    if !skip_condition {
        expr: Expression = ast_create_expr(ast, scope, true);
        condition = calloc(1, sizeof(Expression));
        memcpy(condition, &expr, sizeof(Expression));
    }
    ast_match(ast, TK_OPEN_CURLY, #FILE, #LINE);
    advance(ast);
    if_block: Block_Node = create_block(ast, false, scope);
    ast_match(ast, TK_CLOSE_CURLY, #FILE, #LINE);
    peek = get_peek(ast);
    next: *If_Node = 0;
    if peek.tt == TK_ELSE {
        advance(ast);
        next_if: If_Node = ast_create_if(ast, scope);
        next = calloc(1, sizeof(If_Node));
        memcpy(next, &next_if, sizeof(If_Node));
    }
    if_node: If_Node = If_Node{ if_block, condition, next, is_else };
    return if_node;
}

is_operator: proc(token: *Token) -> bool {
    if token.tt == TK_GT {
        return true;
    } else if token.tt == TK_GTE {
        return true;
    } else if token.tt == TK_LT {
        return true;
    } else if token.tt == TK_LTE {
        return true;
    } else if token.tt == TK_ASSIGN {
        return true;
    } else if token.tt == TK_ADD_ASSIGN {
        return true;
    } else if token.tt == TK_SUB_ASSIGN {
        return true;
    } else if token.tt == TK_PLUS {
        return true;
    } else if token.tt == TK_DOT {
        return true;
    } else if token.tt == TK_LOGICAL_OR {
        return true;
    } else if token.tt == TK_LOGICAL_AND {
        return true;
    } else if token.tt == TK_OPEN_SQUARE {
        return true;
    } else if token.tt == TK_AMPERSAND {
        return true;
    } else if token.tt == TK_OR {
        return true;
    } else if token.tt == TK_NOT {
        return true;
    } else if token.tt == TK_LEFT_SHIFT {
        return true;
    } else if token.tt == TK_RIGHT_SHIFT {
        return true;
    } else if token.tt == TK_SLASH {
        return true;
    } else if token.tt == TK_STAR {
        return true;
    } else {
        return false;
    }
}

copy_statement: proc(stmt: Statement) -> *Statement {
    stmt_alloc: *Statement = calloc(1, sizeof(Statement));
    memcpy(stmt_alloc, &stmt, sizeof(Statement));
    return stmt_alloc;
}

add_current_directory: proc(ast: *Ast, filename: string) -> string {
    // attempt to try to get the correct directory
    // TODO/NOTE: this is not a great solution but works for now
    filepath_tmp: Token = ast.tokens[0];
    filepath_tmp_str: string = filepath_tmp.filename;
    cut_index: i32 = 0;
    for i: i32 = 0; i < filepath_tmp_str.length; i = i + 1 {
        if filepath_tmp_str.ptr[i] == '/' {
            cut_index = i;  
        }
    }

    ret: string;
    if cut_index != 0 {
        sb: String_Builder;
        for i: i32 = 0; i < cut_index + 1; i = i + 1 {
            sb_push(&sb, filepath_tmp_str.ptr[i]);
        }
        for i: i32 = 0; i < filename.length; i = i + 1 {
            sb_push(&sb, filename.ptr[i]);
        }

        ret = sb_to_string(&sb);
        sb_free(&sb);
    } else {
        ret = filename;
    }

    return ret;
}

create_statement: proc(ast: *Ast, scope: u32) -> *Statement {
    current_token: *Token = get_current_token(ast);
    stmt_ret: *Statement = 0;
    root: bool = false;
    if scope == 0 {
        root = true;
    }
    if current_token.tt == TK_WORD {
        rhs: *Expression = 0;
        // expression or Decl
        name: *Token = current_token;
        peek: *Token = get_peek(ast);
        if peek.tt == TK_COLON {
            advance(ast);
            advance(ast);
            // Declaration
            stmt_type: Type = extract_type(ast);
            skip: bool = false;
            // rhs_check is to make sure to check for rhs 
            // when necessary
            rhs_check: bool = true;
            match type_ := stmt_type {
                Type_Builtin => {
                    // do nothing
                }
                Type_Pointer => {
                    // do nothing
                }
                Type_Proc => {
                    type_.name = name;
                    proc_header: Proc_Header_Node = Proc_Header_Node{ 
                        name, 
                        type_.args, 
                        type_.return_type 
                    };
                    array_append(ast.procs, proc_header);
                    rhs_check = false;
                }
                Type_Array => {
                    // do nothing
                }
                Type_Dynamic_Array => {
                    todo("Type_Dynamic_Array not implemented yet", #FILE, #LINE);
                }
                Type_Choice => {
                    todo("Type_Choice not implemented yet", #FILE, #LINE);
                }
                Type_Custom_Decl => {
                    type_.name = name;
                    array_append(ast.types, stmt_type);
                    skip = true;
                    rhs_check = false;
                }
                Type_Custom => {
                    // do nothing
                }
            }
            // check if rhs needs to be parsed
            ct: *Token = get_current_token(ast); // ct = current_token
            if ct.tt == TK_ASSIGN && rhs_check {
                ast_match(ast, TK_ASSIGN, #FILE, #LINE);
                advance(ast);
                r: Expression = ast_create_expr(ast, scope, false);
                rhs = calloc(1, sizeof(Expression));
                memcpy(rhs, &r, sizeof(Expression));
                ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
            }
            if !skip {
                // we have this because we don't want to add Type_Custom_Decl into the list of statements, they are added to the Ast state and will be generated seperately during the codegen phase 
                var_decl: Variable_Decl_Node = Variable_Decl_Node{ name, rhs, stmt_type, false };
                decl: Decl_Node = Decl_Node{ var_decl };
                stmt: Statement = Statement{ decl };
                stmt_ret = copy_statement(stmt);
            }
        } else if peek.tt == TK_OPEN_PAREN {
            assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
            expr: Expression = ast_create_expr(ast, scope, false);
            expr_stmt: Expression_Statement = Expression_Statement{ expr };
            stmt: Statement = Statement{ expr_stmt };
            stmt_ret = copy_statement(stmt);
        } else if is_operator(peek) || peek.tt == TK_OPEN_PAREN {
            if root {
                print_token(current_token);
            }
            assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
            expr: Expression = ast_create_expr(ast, scope, false);
            expr_stmt: Expression_Statement = Expression_Statement{ expr };
            stmt: Statement = Statement{ expr_stmt };
            stmt_ret = copy_statement(stmt);
        } else {
            tmp: string = current_token.tok;
            printf("Unexpected Token: '%s'\n".ptr, tmp.ptr);
            print_token(current_token);
            panic("", #FILE, #LINE);
        }
    } else if current_token.tt == TK_RETURN {
        advance(ast);
        current_token = get_current_token(ast);
        expr: *Expression = 0;
        if current_token.tt != TK_SEMICOLON {
            expr_tmp: Expression = ast_create_expr(ast, scope, false);
            expr = calloc(1, sizeof(Expression));
            return_node: Return_Node = Return_Node{ expr };
            memcpy(return_node.expr, &expr_tmp, sizeof(Expression));
            ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
            stmt: Statement = Statement{ return_node };
            stmt_ret = copy_statement(stmt);
        } else {
            return_node: Return_Node = Return_Node{ expr };
            ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
            stmt: Statement = Statement{ return_node };
            stmt_ret = copy_statement(stmt);
        }
    } else if current_token.tt == TK_OPEN_PAREN {
        assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
        expr: Expression = ast_create_expr(ast, scope, false);
        expr_stmt: Expression_Statement = Expression_Statement{ expr };
        stmt: Statement = Statement{ expr_stmt };
        stmt_ret = copy_statement(stmt);
    } else if current_token.tt == TK_IF {
        if_node: If_Node = ast_create_if(ast, scope);
        expr_stmt_with_block: Expression_Statement_With_Block = Expression_Statement_With_Block{ if_node };
        expr_stmt: Expression_Statement = Expression_Statement{ expr_stmt_with_block };
        stmt: Statement = Statement{ expr_stmt };
        stmt_ret = copy_statement(stmt);
    } else if current_token.tt == TK_FOR {
        for_node: For_Node = ast_create_for(ast, scope);
        expr_stmt_with_block: Expression_Statement_With_Block = Expression_Statement_With_Block{ for_node };
        expr_stmt: Expression_Statement = Expression_Statement{ expr_stmt_with_block };
        stmt: Statement = Statement{ expr_stmt };
        stmt_ret = copy_statement(stmt);
    } else if current_token.tt == TK_INCLUDE {
        advance(ast);
        filepath: *Token = get_current_token(ast);
        advance(ast);
        ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
        filepath_str: string = filepath.tok;
        new_filename: string = add_current_directory(ast, filepath_str);
        
        file_src: string = read_file_to_string(new_filename);
        // TODO: take the path of the current file into account like the rust version does
        // TODO: FIX this
        tokens: [+]Token = tokenize(&file_src, filepath_str);
        for i: i32 = array_size(tokens) - 1; i >= 0; i = i - 1 {
            // we doin plus 1 because we want to place the tokens after the index
            array_insert(ast.tokens, tokens[i], ast.index + 1);
        }
    
        // TODO: remove, this is debug printing to check that include works
        for i: i32 = 0; i < array_size(ast.tokens); i = i + 1 {
            print_token(&ast.tokens[i]);
        }
    } else {
        print_token(current_token);
        tmp: string = current_token.tok;
        printf("Unexpected Token: '%s'\n".ptr, tmp.ptr);
        panic("", #FILE, #LINE);
    }
    return stmt_ret;
}

create_block: proc(ast: *Ast, root: bool, parent_scope: u32) -> Block_Node {
	block: Block_Node;
	block.statements = array_new(Statement);
	block.scope = create_new_scope(ast, parent_scope);
	ast.current_scope = block.scope;

	defers: [+]Statement; // TODO: work on
	for true {
		current_token: *Token = get_current_token(ast);
		if current_token.tt == TK_CLOSE_CURLY {
			if root {
				panic("[ERROR]: Unexpected Close Curly Bracket found in top level scope", #FILE, #LINE);
			} else {
				break;
			}
		}
		// if current_token.tt == TK_WORD {
		//           rhs: *Expression = 0;
		// 	// expression or Decl
		// 	name: *Token = current_token;
		// 	peek: *Token = get_peek(ast);
		// 	if peek.tt == TK_COLON {
		// 		advance(ast);
		// 		advance(ast);
		// 		// Declaration
		// 		stmt_type: Type = extract_type(ast);
		//               skip: bool = false;
		//               // rhs_check is to make sure to check for rhs 
		//               // when necessary
		//               rhs_check: bool = true;
		// 		match type_ := stmt_type {
		// 			Type_Builtin => {
		//                       // do nothing
		// 			}
		// 			Type_Pointer => {
		// 				todo("Type_Pointer not implemented yet", #FILE, #LINE);
		// 			}
		// 			Type_Proc => {
		// 				type_.name = name;
		//                       proc_header: Proc_Header_Node = Proc_Header_Node{ 
		//                           name, 
		//                           type_.args, 
		//                           type_.return_type 
		//                       };
		//                       array_append(ast.procs, proc_header);
		//                       rhs_check = false;
		// 			}
		// 			Type_Array => {
		//                       // do nothing
		// 			}
		// 			Type_Dynamic_Array => {
		// 				todo("Type_Dynamic_Array not implemented yet", #FILE, #LINE);
		// 			}
		// 			Type_Choice => {
		// 				todo("Type_Choice not implemented yet", #FILE, #LINE);
		// 			}
		// 			Type_Custom_Decl => {
		//                       type_.name = name;
		//                       array_append(ast.types, stmt_type);
		//                       skip = true;
		//                       rhs_check = false;
		// 			}
		//                   Type_Custom => {
		//                       // do nothing
		//                   }
		// 		}
		//               // check if rhs needs to be parsed
		//               ct: *Token = get_current_token(ast); // ct = current_token
		//               if ct.tt == TK_ASSIGN && rhs_check {
		//                   ast_match(ast, TK_ASSIGN, #FILE, #LINE);
		//                   advance(ast);
		//                   r: Expression = ast_create_expr(ast, block.scope, false);
		//                   rhs = calloc(1, sizeof(Expression));
		//                   memcpy(rhs, &r, sizeof(Expression));
		//                   ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
		//               }
		//               if !skip {
		//                   // we have this because we don't want to add Type_Custom_Decl into the list of statements, they are added to the Ast state and will be generated seperately during the codegen phase 
		//                   var_decl: Variable_Decl_Node = Variable_Decl_Node{ name, rhs, stmt_type, false };
		//                   decl: Decl_Node = Decl_Node{ var_decl };
		//                   stmt: Statement = Statement{ decl };
		//                   array_append(block.statements, stmt);
		//               }
		// 	} else if peek.tt == TK_OPEN_PAREN {
		//               assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
		//               expr: Expression = ast_create_expr(ast, block.scope, false);
		//               expr_stmt: Expression_Statement = Expression_Statement{ expr };
		//               stmt: Statement = Statement{ expr_stmt };
		//               array_append(block.statements, stmt);
		//           } else if is_operator(peek) || peek.tt == TK_OPEN_PAREN {
		//               if root {
		//                   print_token(current_token);
		//               }
		//               assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
		//               expr: Expression = ast_create_expr(ast, block.scope, false);
		//               expr_stmt: Expression_Statement = Expression_Statement{ expr };
		//               stmt: Statement = Statement{ expr_stmt };
		//               array_append(block.statements, stmt);
		//           } else {
		//               tmp: string = current_token.tok;
		//               printf("Unexpected Token: '%s'\n".ptr, tmp.ptr);
		//               print_token(current_token);
		//               panic("", #FILE, #LINE);
		//           }
		// } else if current_token.tt == TK_RETURN {
		//           advance(ast);
		//           current_token = get_current_token(ast);
		//           expr: *Expression = 0;
		//           if current_token.tt != TK_SEMICOLON {
		//               expr_tmp: Expression = ast_create_expr(ast, block.scope, false);
		//               expr = calloc(1, sizeof(Expression));
		//               return_node: Return_Node = Return_Node{ expr };
		//               memcpy(return_node.expr, &expr_tmp, sizeof(Expression));
		//               ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
		//               stmt: Statement = Statement{ return_node };
		//               array_append(block.statements, stmt);
		//           } else {
		//               return_node: Return_Node = Return_Node{ expr };
		//               ast_match(ast, TK_SEMICOLON, #FILE, #LINE);
		//               stmt: Statement = Statement{ return_node };
		//               array_append(block.statements, stmt);
		//           }
		//       } else if current_token.tt == TK_OPEN_PAREN {
		//           assert(!root, "Expressions are not allowed in the top level scope", #FILE, #LINE);
		//           expr: Expression = ast_create_expr(ast, block.scope, false);
		//           expr_stmt: Expression_Statement = Expression_Statement{ expr };
		//           stmt: Statement = Statement{ expr_stmt };
		//           array_append(block.statements, stmt);
		//       } else if current_token.tt == TK_IF {
		//           if_node: If_Node = ast_create_if(ast, block.scope);
		//           expr_stmt_with_block: Expression_Statement_With_Block = Expression_Statement_With_Block{ if_node };
		//           expr_stmt: Expression_Statement = Expression_Statement{ expr_stmt_with_block };
		//           stmt: Statement = Statement{ expr_stmt };
		//           array_append(block.statements, stmt);
		//       } else if current_token.tt == TK_FOR {
		//           for_node: For_Node = ast_create_for(ast, block.scope);
		//           expr_stmt_with_block: Expression_Statement_With_Block = Expression_Statement_With_Block{ for_node };
		//           expr_stmt: Expression_Statement = Expression_Statement{ expr_stmt_with_block };
		//           stmt: Statement = Statement{ expr_stmt };
		//           array_append(block.statements, stmt);
		//       } else {
		//           tmp: string = current_token.tok;
		//           printf("Unexpected Token: '%s'\n".ptr, tmp.ptr);
		//           panic("", #FILE, #LINE);
		//       }
        
        stmt: *Statement = create_statement(ast, block.scope);
        if stmt != 0 {
            array_append(block.statements, stmt[0]);
        }
		advance(ast);
		if ast.index >= array_size(ast.tokens) {
			break;
		}
	}

	if root {
		print_ast(block);
	}
    ast.current_scope = parent_scope;
	return block;
}

print_indentations: proc(n: i32) {
	sb: String_Builder;
	for i: i32 = 0; i < n; i = i + 1 {
		sb_push(&sb, ' ');
	}
	sb_push(&sb, 0);
	printf("%s".ptr, sb.chars);
	sb_free(&sb);
}

print_expression: proc(expr_: *Expression, indentations: i32) {
    expr: *Expression = expr_; // NOTE/TODO: have to do this because of bug with compiler
    match e := expr {
        Binary_Node => {
            print_indentations(indentations);
            op_str: string = get_op_string(e.op);
            printf("BinaryOp: %s\n".ptr, op_str.ptr);
            print_indentations(indentations + 2);
            printf("lhs:\n".ptr);
            print_expression(e.lhs, indentations + 4);
            print_indentations(indentations + 2);
            printf("rhs:\n".ptr);
            print_expression(e.rhs, indentations + 4);
            // print_indentations(indentations);
            // printf("Binary_Node: %zu\n".ptr, e.val);
        },
        Number_Node => {
            print_indentations(indentations);
            printf("Number: %zu\n".ptr, e.val);
        },
        Variable_Node => {
            print_indentations(indentations);
            // NOTE/TODO: redundant variables due to needing being unable to chain access operator together lol
            token: *Token = e.symbol;
            str: string = token.tok; 
            printf("Number: %\n".ptr, str.ptr);
        },
        Call_Node => {
            print_indentations(indentations);
            name: *Token = e.name;
            name_str: string = name.tok;
            printf("Call: %s\n".ptr, name_str.ptr);
            for i: i32 = 0; i < array_size(e.args); i = i + 1 {
                arg: Expression = e.args[i];
                print_expression(&arg, indentations + 2);
            }
        },
        String_Literal => {
            string_literal: string = e.str;
            print_indentations(indentations);
            printf("String: %s\n".ptr, string_literal.ptr);
        },
        Unary_Node => {
            print_indentations(indentations);
            op_str: string = get_op_string(e.op);
            printf("UnaryOp: %s\n".ptr, op_str.ptr);
            print_indentations(indentations + 2);
            printf("expr:\n".ptr);
            print_expression(e.base, indentations + 4);
        },
        Struct_Init_Node => {
                //StructDeclaration(Some(Token { x: 23, y: 16, tok: "Vector2", tt: Word, filename: "hello.ce" })):
                //    Number: 1
                //    Number: 2
            print_indentations(indentations);
            if e.name == 0 {
                printf("Struct Declaration:\n".ptr);
            } else {
                name_tok: *Token = e.name;
                name_str: string = name_tok.tok;
                printf("Struct Initialization '%s':\n".ptr, name_str.ptr);
            }
            for i: i32 = 0; i < array_size(e.exprs); i = i + 1 {
                print_expression(&e.exprs[i], indentations + 2);
            }
        },
        Array_Literal => {
            print_indentations(indentations);
            printf("ArrayLiteral: [\n".ptr);
            for i: i32 = 0; i < array_size(e.elements); i = i + 1 {
                print_expression(&e.elements[i], indentations + 2);
            }
            print_indentations(indentations);
            printf("]\n".ptr);
        }
    }
}

print_decl: proc(decl: *Decl_Node, indentations: i32) {
    // TODO: this proc has a lot of duplicate code which needs to be refactored
    //       refactor once dev DX is better lol
    // TODO: these prints are not actually working, need to fix it
	tmp: *Decl_Node = decl;
	match decl_ := tmp {
		Variable_Decl_Node => {
			decl_type: Type = decl_.var_type;
			match var_decl_type := decl_type {
				Type_Builtin => {
					print_indentations(indentations + 2);
					printf("Variable Declaration:\n".ptr);
					print_indentations(indentations + 4);
					token_symbol: *Token = decl_.symbol;
					name_str: string = token_symbol.tok;
					printf("name: '%s'\n".ptr, name_str.ptr);
                    // TODO: print the type
                    if decl_.rhs != 0 {
                        print_indentations(indentations + 4);
                        printf("Initializer:\n".ptr);
                        print_expression(decl_.rhs, indentations + 6);
                        // todo("Type_Builtin printing rhs (print_decl)", #FILE, #LINE);
                    } else {
                        print_indentations(indentations + 4);
                        printf("Initializer: (none)\n".ptr);
                    }
				}
				Type_Pointer => {
					print_indentations(indentations + 2);
					printf("Variable Declaration:\n".ptr);
					print_indentations(indentations + 4);
					token_symbol: *Token = decl_.symbol;
					name_str: string = token_symbol.tok;
					printf("name: '%s'\n".ptr, name_str.ptr);
                    // TODO: print the type
                    if decl_.rhs != 0 {
                        print_indentations(indentations + 4);
                        printf("Initializer:\n".ptr);
                        print_expression(decl_.rhs, indentations + 6);
                        // todo("Type_Builtin printing rhs (print_decl)", #FILE, #LINE);
                    } else {
                        print_indentations(indentations + 4);
                        printf("Initializer: (none)\n".ptr);
                    }
				}
				Type_Proc => {
					print_indentations(indentations + 2);
					printf("Proc Declaration:\n".ptr);
					print_indentations(indentations + 4);
					token_symbol: *Token = var_decl_type.name;
					name_str: string = token_symbol.tok;
					printf("name: '%s'\n".ptr, name_str.ptr);
					if var_decl_type.return_type != 0 {
                        print_indentations(indentations + 4);
                        // TODO: using [0] for dereference lol
                        type_string: string = get_type_string(var_decl_type.return_type[0]);
						printf("return type: %s\n".ptr, type_string.ptr);
					} else {
						print_indentations(indentations + 4);
						printf("return type: (nil)\n".ptr);
					}

					print_indentations(indentations + 4);
					printf("body:\n".ptr);
					print_block(&var_decl_type.block, indentations + 6);
				}
				Type_Array => {
					print_indentations(indentations + 2);
					printf("Variable Declaration:\n".ptr);
					print_indentations(indentations + 4);
					token_symbol: *Token = decl_.symbol;
					name_str: string = token_symbol.tok;
					printf("name: '%s'\n".ptr, name_str.ptr);
                    // TODO: print the type
                    if decl_.rhs != 0 {
                        print_indentations(indentations + 4);
                        printf("Initializer:\n".ptr);
                        print_expression(decl_.rhs, indentations + 6);
                        // todo("Type_Builtin printing rhs (print_decl)", #FILE, #LINE);
                    } else {
                        print_indentations(indentations + 4);
                        printf("Initializer: (none)\n".ptr);
                    }
				}
				Type_Dynamic_Array => {
					todo("Type_Dynamic_Array not implemented (print_decl)", #FILE, #LINE);
				}
				Type_Choice => {
					todo("Type_Choice not implemented (print_decl)", #FILE, #LINE);
				}
                Type_Custom_Decl => {
                    print_indentations(indentations + 2);
                    name_tok: *Token = var_decl_type.name;
                    name_str: string = name_tok.tok;
                    printf("Type Declaration: %s\n".ptr, name_str.ptr);
                    for i: i32 = 0; i < array_size(var_decl_type.fields); i = i + 1 {
                        field: Field = var_decl_type.fields[i];
                        field_token: *Token = field.name;
                        field_str: string = field_token.tok;
                        print_indentations(indentations + 4);
                        t: string = get_type_string(field.type_name);
                        printf("%s: %s\n".ptr, field_str.ptr, t.ptr);
                    }
                    unreachable("Shouldn't have any struct declarations here", #FILE, #LINE);
                },
				Type_Custom => {
                    print_indentations(indentations + 2);
                    name_tok: *Token = var_decl_type.name;
					name_str: string = name_tok.tok;
					printf("name: '%s'\n".ptr, name_str.ptr);
                    type_str: string = name_tok.tok;
                    print_indentations(indentations + 2);
                    printf("Custom Type: %s\n".ptr, type_str.ptr);
                    // TODO: print the type
                    if decl_.rhs != 0 {
                        print_indentations(indentations + 4);
                        printf("Initializer:\n".ptr);
                        print_expression(decl_.rhs, indentations + 6);
                        // todo("Type_Builtin printing rhs (print_decl)", #FILE, #LINE);
                    } else {
                        print_indentations(indentations + 4);
                        printf("Initializer: (none)\n".ptr);
                    }
				},
			}
		}
	}
}

print_if_statement: proc(if_node: *If_Node, indentations: i32) {
    if if_node.condition != 0 {
        print_indentations(indentations);
        printf("If Statement:\n".ptr);
        print_indentations(indentations + 2);
        printf("Condition:\n".ptr);
        print_expression(if_node.condition, indentations + 2);
    } else {
        print_indentations(indentations);
        printf("Else:\n".ptr);
    }
    print_block(&if_node.block, indentations + 2);
    if if_node.next != 0 {
        print_if_statement(if_node.next, indentations);
    } 
}

print_for: proc(for_node: *For_Node, indentations: i32) {
     print_indentations(indentations);
     printf("For Statement (TODO):\n".ptr);
     print_indentations(indentations);
     if for_node.is_classic_for {
         // todo("printing for classical for loops", #FILE, #LINE);
     }
     printf("Condition:\n".ptr);
     if for_node.condition {
        print_expression(for_node.condition, indentations + 2);
     } else {
        printf("(none)\n".ptr);
     }
     print_block(&for_node.block, indentations);
}

print_expression_with_block: proc(expr_block_: *Expression_Statement_With_Block, indentations: i32) {
    print_indentations(indentations);
    printf("Expression Statement (with block): \n".ptr);
    expr_block: *Expression_Statement_With_Block = expr_block_;
    match e_block := expr_block {
        If_Node => {
            print_if_statement(e_block, indentations);
        },
        For_Node => {
            print_for(e_block, indentations);
        }
    }
}

print_block: proc(block: *Block_Node, indentations: i32) {
	print_indentations(indentations);
	printf("Block {\n".ptr);
	for i: i32 = 0; i < array_size(block.statements); i = i + 1 {
		 print_indentations(indentations + 2);
		 printf("[%d]\n".ptr, i);
		 stmt: Statement = block.statements[i];
		 match type_ := stmt {
		 	Expression_Statement => {
                // TODO/NOTE: doing this declaration because type_ is not added in the scope
                t: *Expression_Statement = type_;
                print_indentations(indentations + 2);
                printf("Expression Statement:\n".ptr);
                match e := t {
                    Expression => {
                        print_expression(e, indentations + 4);
                    },
                    Expression_Statement_With_Block => {
                        print_expression_with_block(e, indentations + 4);
                    }
                }
		 	},
		 	Decl_Node => {
				print_decl(type_, indentations + 4);
		 	},
            Return_Node => {
                print_indentations(indentations + 4);
		 		printf("Return: \n".ptr);
                print_expression(type_.expr, indentations + 6);
            }
		 }
	}	
	print_indentations(indentations);
	printf("}\n".ptr);
}

print_ast: proc(root: Block_Node) {
	printf("===== PRINTING AST ======\n".ptr);
	indentations: i32 = 0;

	for i: i32 = 0; i < array_size(root.statements); i = i + 1 {
		 print_indentations(indentations + 2);
		 printf("[%d]\n".ptr, i);
		 stmt: Statement = root.statements[i];
		 match type_ := stmt {
		 	Expression_Statement => {
		 		todo("print_ast for Expression_Statement", #FILE, #LINE);
		 	},
		 	Decl_Node => {
				print_decl(type_, indentations + 2);
		 	},
            Return_Node => {
                unreachable("There should not be returns at the top level scope", #FILE, #LINE);
            }
		 }
	}	
	
	printf("=========================\n".ptr);
}

ast_start: proc(tokens: [+]Token) -> Ast {
	ast: Ast;
	// init ast
	ast.tokens = tokens;
    ast.strings_index = 0;
	ast.index = 0;
	ast.scopes = array_new(Scope);
	ast.types = array_new(Type);
    ast.procs = array_new(Proc_Header_Node);
	ast.strings = array_new(String_Literal);
	ast.root_block = create_block(&ast, true, 0 - 1);
	
    return ast;
}
